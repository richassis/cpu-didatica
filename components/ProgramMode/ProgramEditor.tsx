"use client";

/**
 * ProgramEditor — Legacy wrapper.
 * The primary assembly editing UI is now AssemblyPanel.
 * This component is kept for export compatibility but the layout
 * uses AssemblyPanel directly.
 */
export { default } from "@/components/ProgramMode/AssemblyPanel";
